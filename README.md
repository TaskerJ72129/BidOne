# BidOne Technical Assessment - MVC Version

## Overview
This is a simple ASP.NET Core MVC application that collects a user's first and last name via a form and stores each submission in a local JSON file.

The purpose of this project is to demonstrate basic MVC structure, form submission, and simple file-based persistence.

## How to run

```bash
cd TechnicalAssessment
dotnet run
```
